{
    "num_index_replicas": 0,
    "bucket": "travel",
    "scopes": {
       "travel": {
          "collections": {
             "airlines": { "sync": "function (doc, oldDoc, meta) { channel(doc.channels); }" },
             "routes": { "sync": "function (doc, oldDoc, meta) { channel(doc.channels); }" },
             "airports": { "sync": "function (doc, oldDoc, meta) { channel(doc.channels); }" },
             "landmarks": { "sync": "function (doc, oldDoc, meta) { channel(doc.channels); }" },
             "hotels": { "sync": "function (doc, oldDoc, meta) { channel(doc.channels); }" }
          }
       }
    },
    "delta_sync": {
        "enabled": true
    }
 }
 