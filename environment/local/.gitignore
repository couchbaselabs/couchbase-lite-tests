# Build artifacts for windows
*.exe

# Sync Gateway local builds
sync_gateway
sync_gateway_clone/
*.log

# Generated by start_local.py. Every generated name gets a suffix after the template's own name,
# so the patterns cannot swallow the tracked templates they are patched from.
topology_config
topology_configs/*_config_*.json
sync_gateway_config/basic_sync_gateway_cbs_*.json
sync_gateway_config/sync_gateway_instance*.json

# Generated by start_local.py --start-cbs (via cbdinocluster's integration-test/start_cbs.py)
.cbdinocluster-sg-cluster-id
